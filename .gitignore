.python-version
uv.lock
.DS_Store
.idea/
*.egg-info/
*.pyc
__pycache__/
venv/
.venv/
.pbg/
# Anchor workspace-level artifact dirs to the repo root so the same names
# nested inside the published dashboard bundle (doc/dashboard/reports/, ...)
# are NOT ignored.
/out/
/reports/
/scripts/
/studies/*/runs.db
